import { StyleSheet } from 'react-native';

const appStyles = StyleSheet.create({
  globalMargin: {
    marginHorizontal: 20,
  },
  pokeball: {
    position: 'absolute',
    top: -100,
    right: -100,
    opacity: 0.2,
    width: 300,
    height: 300,
  },
  title: {
    fontSize: 35,
  },
});

export default appStyles;
